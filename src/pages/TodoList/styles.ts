import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  todoList: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    padding: 40
  }
});
